# DECISIONS.md — SAMS Academy

Log of ambiguous-requirement calls and accepted-risk notes made autonomously per `CLAUDE.md §3`. Newest last.

## 2026-07-31 — Frontend integration kickoff

**Context:** `client/` already contains the Google AI Studio export (built from `docs/11_AISTUDIO_FRONTEND_PROMPTS.md`), including `assets/.aistudio`, a `bun.lock`, and a mock API layer in every `client/src/api/endpoints/*.ts` module that mirrors `04_API_SPEC.md`. Backend (`server/`) has not been started yet (Phase 0 pending).

1. **Source of truth for the frontend:** the exported `client/` is treated as a finished deliverable, not a scaffold. No rebuild, no restructuring of pages/components. Integration work means: flip the mock flag, verify each real endpoint call matches `04_API_SPEC.md`, and fix drift on the **backend** side wherever the frontend's existing TypeScript types are reasonable. See `CLAUDE.md §1a`.
2. **Config flip:** `client/src/config.ts` → `CONFIG.USE_MOCK = false`. `CONFIG.API_BASE_URL` was already `"/api/v1"` (no change). `client/src/api/client.ts`'s `apiFetch` already sends `credentials: "include"` unconditionally (no change).
3. **07_EXECUTION_PLAN.md re-scope:** every frontend-build sub-task in Phases 3–11 reworded from "build the UI" to "wire the exported UI to the real API + fix contract drift." Backend, security, and test sub-tasks in those phases are untouched. See the plan's own "Frontend re-scope" note under the Protocol line.
4. **Video/HLS (`Phase 5`):** `client/src/components/player/SecurePlayer.tsx` and `client/src/api/endpoints/video.ts` already call the real endpoints (`GET /student/lectures/:id/play`, `PUT .../heartbeat`, `POST .../complete`) when `USE_MOCK` is false — no frontend code change was needed for the mock→real swap itself. **Contract-drift item flagged for Phase 5.2 (backend):** the frontend's `PlaybackConfig` type uses `resumeAtSeconds` where `04_API_SPEC.md`'s shorthand says `resumeAt`, and `watermark.timestamp` where the spec's shorthand says `watermark.now`. Decision: the backend's `/play` response must emit `resumeAtSeconds` and `watermark.timestamp` to match the already-built, working frontend, rather than editing the frontend to match the doc's shorthand names.
5. **Phase 13 QA matrix (`08_TESTING_QA.md`):** applies unchanged — it tests backend contracts and manual UI behavior, neither of which depends on the UI's origin (AI Studio export vs. hand-built).
6. **Known drift, deferred:** `client/package.json` / `client/.env.example` still carry Google AI Studio template boilerplate unrelated to SAMS (`@google/genai` dependency, its own `express` dev server/`server.js`, `GEMINI_API_KEY`/`APP_URL` env vars). Left as-is for this kickoff (out of requested scope); revisit during Phase 14.2 (`.env.example` finalization) / 14.3 (deploy zip — must not ship dead deps).

   **Resolved 2026-07-31:** confirmed zero usage of `@google/genai`/Gemini anywhere in `client/src` (this is a medical exam-prep LMS, not a Gemini app) and no `server.js` was ever present in this export. Removed from `client/package.json`: `@google/genai`, `express`, `dotenv` (dependencies); `esbuild`, `tsx`, `@types/express` (devDependencies); also deduped the `vite` entry that was listed under both `dependencies` and `devDependencies`, and dropped the dead `server.js` reference from the `clean` script. Deleted `client/bun.lock` (AI Studio's default package manager; this project standardizes on npm/`package-lock.json` per `CLAUDE.md`). Rewrote `client/.env.example` — `client/src` has zero `import.meta.env`/`process.env` reads, so it now just documents that no client build-time env vars are needed (`CONFIG.API_BASE_URL` is hardcoded same-origin). Clean reinstall dropped 262→138 packages; `npm run lint` and `npm run build` both pass, and the build output hash is byte-identical to before the cleanup, confirming nothing in the bundle depended on the removed packages. Left untouched at first (different category, not originally asked): `client/metadata.json`'s `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API` flag, `client/README.md`'s AI Studio banner/link, and `client/assets/.aistudio/` — cosmetic/identity artifacts, not dependency or scaffold cleanup.

   **Also resolved 2026-07-31 (follow-up):** deleted `client/metadata.json` (AI Studio project metadata — name/description/capability flags for the AI Studio platform UI; unreferenced by the build) and `client/assets/` (contained only `.aistudio/.gitignore`, an AI-Studio asset-cache marker with no other files, unreferenced anywhere in `client/src`). Rewrote `client/README.md` to describe the actual SAMS Academy client instead of the generic "Run and deploy your AI Studio app" banner. `npm run build` output hash unchanged both times, confirming none of these were load-bearing.

## 2026-07-31 — Phase 0 bootstrap (devops-docs)

**Context:** stood up `server/` per `docs/02_ARCHITECTURE.md §2`, root tooling, and the client's test/lint wiring (`docs/07_EXECUTION_PLAN.md` Phase 0.1–0.6).

1. **Root `package.json`: `concurrently`, not npm workspaces.** `client/` and `server/` each keep their own independent `package.json`/`node_modules`/lockfile, orchestrated from the root via `concurrently` (dev) and `--prefix` (build/migrate/seed/lint/test). Chosen over npm workspaces because: (a) `client/` is a finished, externally-exported deliverable (Google AI Studio output, see the kickoff entry above) whose dependency tree (React 19, Vite 6, Tailwind 4) should stay fully isolated from the server's (Express 4, Sequelize 6, CommonJS-ish tooling) — a shared workspace root `node_modules` risks silent hoisting/version-resolution surprises across two very different stacks; (b) Hostinger's Node-app deploy model runs `npm run build` then `npm start` from the repo root with a single `package.json` contract — `--prefix`-based scripts satisfy that with zero extra tooling; (c) workspaces would force a single top-level lockfile mixing both stacks' transitive deps, complicating the Phase 14 `deploy.zip` builder (which must ship `server/node_modules` — production deps only — but never `client/node_modules`, since the client only ships its built `dist/`).
2. **Server language: ES modules JS, confirmed.** Per `CLAUDE.md §1a`/`§4` ("pick one at Phase 0 and stay consistent" — already decided as ES modules JS for the server). `server/package.json` sets `"type": "module"`; all `server/src/**/*.js` use `import`/`export`. The one deliberate exception: `server/.sequelizerc` and `server/src/db/config.cjs` are plain CommonJS (`.cjs` extension / `require`/`module.exports`), because `sequelize-cli` loads both via a raw `require()` call that cannot load ESM synchronously — `.cjs` sidesteps the package's `"type": "module"` regardless of file location. Verified empirically: `npx sequelize-cli db:migrate --env test` from `server/` correctly loads `src\db\config.cjs` and reaches a real `ECONNREFUSED 127.0.0.1:3306` (i.e. fails only on "no MySQL running", not on config loading) — confirming the `.sequelizerc` → `.cjs` config path works under `"type": "module"`. Phase 1's actual migration/seeder files will need the same `.cjs` treatment (or an equivalent ESM-compatible sequelize-cli version) — flagged here so Phase 1 doesn't rediscover this.
3. **Node version: target 20, local machine has 25.** `CLAUDE.md` and Hostinger both require Node 20 LTS, but this dev machine only has Node v25.6.1 installed (no Node 20 available locally). Both `package.json` files declare `"engines": {"node": ">=20"}` (not a hard pin to `20.x`, which would fail the local engine check and block `npm install`/`npm run verify` entirely here). All Phase 0 verification in this session therefore ran on Node 25, not Node 20. Accepted as a dev-environment note per the brief; **actually verifying green on Node 20 itself is a Phase 14 follow-up**, not done now.
4. **`npm run package` is a Phase-14 stub.** Root script `"package": "echo \"TODO: Phase 14 deploy.zip builder\" && exit 1"` — intentionally fails (exit 1) rather than silently no-op, so nothing downstream mistakes "the command exists" for "the command works." The real `deploy.zip` builder (per `docs/09_DEPLOYMENT_HOSTINGER.md`) is built in Phase 14.3.
5. **Sequelize naming strategy: `underscored: true` globally.** `docs/03_DATABASE_SCHEMA.md` specifies snake_case columns throughout (`created_at`, `updated_at`, `user_id`, ...). `server/src/db/sequelize.js` sets `define: { underscored: true, timestamps: true }` on the shared Sequelize instance (mirrored in `server/src/db/config.cjs` for sequelize-cli), so Phase 1 models can declare idiomatic camelCase JS attributes (`createdAt`, `userId`) and get the spec's snake_case columns automatically, instead of every model manually specifying `field: 'snake_case_name'` or fighting Sequelize's default camelCase-column behavior.
6. **`storage/` `.gitkeep` handling.** `.gitignore` was extended (not duplicated) so `storage/{uploads,invoices,backups,logs}/*` content stays ignored while each folder's `.gitkeep` is explicitly un-ignored (`!storage/uploads/.gitkeep` etc.) — the five `storage/` subfolders now exist in the repo structure without ever accidentally committing generated content. `storage/dev-assets/` was left un-ignored (its `.gitkeep` is a plain tracked file) since a future phase (5.1, mock video driver) may commit a small sample HLS/mp4 fixture there.
7. **Client test runner: `vitest.config.ts` reuses `vite.config.ts`'s `resolve.alias`** (by invoking the exported config function the same way Vite itself does at build time) rather than redeclaring the `@` alias a second time, so the two configs can't drift. `test.passWithNoTests: true` is set deliberately — Phase 0 wires the runner with zero test files yet, and `npm run verify` needs `client`'s `npm test` to exit 0 in that state; Phase 3+ adding real vitest specs makes this option irrelevant but harmless.

## 2026-07-31 — Local MySQL 8.4 provisioned; DB_NAME_test selection bug fixed (orchestrator)

**Context:** this dev machine had no MySQL/Docker at all. Installed MySQL Server 8.4.9 locally (winget, `Oracle.MySQL`, official `cdn.mysql.com` MSI, hash-verified by winget) rather than a portable/Docker fallback, since Docker isn't available here either. Installed as a genuine Windows service (`MySQL84`, auto-start), not just a foreground process, so it survives across dev sessions without being re-launched manually.

1. **Local instance layout:** binaries at `C:\Program Files\MySQL\MySQL Server 8.4`, datadir at `C:\ProgramData\MySQL\MySQL Server 8.4\Data`, port 3306, service name `MySQL84`. Root has a locally-generated password (not stored in the repo; local-machine-only, given to the user out-of-band). A dedicated least-privilege app user `sams_app` (password in `server/.env`, gitignored) owns two databases: `sams_academy` (dev) and `sams_academy_test` (test) — matching `.env.example`'s documented `DB_NAME`/`DB_NAME_test` defaults exactly, `utf8mb4_unicode_ci`.
2. **Bug found + fixed: `server/src/db/sequelize.js` never actually consumed `DB_NAME_test`.** The Phase-0 handoff defined the `DB_NAME_test` env var and a `health.test.js` smoke test asserting `db:false`, but the Sequelize instance always connected using `env.DB_NAME` regardless of `NODE_ENV` — the `db:false` in the original test was really just an artifact of MySQL being unreachable at the time, not a verified test/dev DB split. Once MySQL came up, the smoke test started connecting to the **dev** database and failing its own `db:false` assertion. Fixed `sequelize.js` to select `env.DB_NAME_test` when `NODE_ENV==='test'` and `env.DB_NAME` otherwise, and updated `health.test.js` to assert `db:true` (per `docs/08_TESTING_QA.md`, the test suite is *supposed* to require a real, reachable, dedicated test MySQL DB — a silently-passing `db:false` would have masked future test-DB misconfiguration instead of failing loudly on it). `npm run verify` is green end-to-end against the live DB after this fix.
3. **Node 20-vs-25 note (§3 above) still stands** — this MySQL work didn't change that; still a Phase 14 follow-up.

## 2026-07-31 — Phase 1 database (db-engineer): migrations, models, seeders

**Context:** implemented `docs/07_EXECUTION_PLAN.md` Phase 1.1–1.6 against `docs/03_DATABASE_SCHEMA.md` — all 34 tables, one Sequelize model per table with associations, and idempotent seeders. Verified against the real local `sams_academy` (dev) and `sams_academy_test` (test) MySQL databases, not just read for correctness.

1. **`.cjs` migrations/seeders confirmed necessary, as Phase 0 flagged.** Both `server/src/db/migrations/*.cjs` and `server/src/db/seeders/*.cjs` are plain CommonJS (`module.exports = { up, down }`), verified empirically by actually running `sequelize-cli db:migrate`/`db:seed:all` against the live dev DB — sequelize-cli's file loader `require()`s these directly regardless of `server/package.json`'s `"type": "module"`, same root cause as `.sequelizerc`/`config.cjs` from Phase 0.
2. **Migrations + models generated from one canonical schema spec, not hand-written per file.** Given 34 tables with heavy structural repetition (id/timestamps boilerplate, FK/index wiring), I transcribed `03_DATABASE_SCHEMA.md`'s DDL 1:1 into a small JS data structure (columns, indexes, FKs, timestamp shape per table) and wrote a generator that emits both the migration files and the model files from that single source of truth, so migrations and models can't drift apart. The generator itself is not part of the deliverable (scratch tooling only); the emitted `.cjs`/`.js` files in `server/src/db/migrations/` and `server/src/models/` are the real, reviewable artifacts and were spot-checked against the literal DDL (`SHOW CREATE TABLE` on `users`, `orders`, `playback_sessions`, `bank_transfer_proofs`, `settings`, `test_attempt_questions` all byte-match the doc's columns, types, defaults, constraint names, and — importantly — the doc's *omissions*: e.g. `playback_sessions.lecture_id`/`device_id` and `test_attempt_questions.selected_option_id` are deliberately left without FK constraints in the literal DDL, and the generated migrations correctly do not add constraints sequelize-cli would otherwise default to).
3. **`models/index.js` auto-loader: standard sequelize-cli-generated pattern, adapted for ESM.** Each model file default-exports `(sequelize) => Model` and defines a static `associate(models)` (sequelize-cli's own convention) instead of centralizing all association wiring in `index.js` itself. `server/src/models/index.js` dynamically `import()`s every `*.js` file in the directory (via `pathToFileURL`, since Node's ESM loader — unlike CommonJS `require()` — rejects bare Windows absolute paths), initializes each model against the shared `sequelize` singleton from `src/db/sequelize.js`, then calls every model's `associate(models)` only after all 34 are loaded, so association definitions can reference any other model regardless of file iteration order.
4. **Real bug found + fixed: Sequelize association `foreignKey` must be the camelCase model *attribute* name, not the raw snake_case DB column.** Initially every `belongsTo`/`hasMany`/`hasOne` call used the literal DDL column name (e.g. `foreignKey: 'user_id'`), matching the schema doc's own naming. This is wrong for a model using `underscored: true`: passing the snake_case string causes Sequelize to silently create a **second, colliding attribute** literally named `user_id` (distinct from the real `userId` attribute the column definition already produces), which breaks the generated `getX()`/`setX()` mixins — confirmed via an isolated repro (`B.belongsTo(A, { foreignKey: 'a_id' })` produces both `aId` and `a_id` in `B.rawAttributes`) and via a failing Jest assertion (`enrollment.getUser()` returned `null`) before the fix. Fixed by using the camelCase attribute name in every association's `foreignKey`/`otherKey` (e.g. `foreignKey: 'userId'`) — the underscored→snake_case column mapping already lives on the attribute's own definition, so associations must reference it by attribute name. All 34 models were regenerated after the fix; `server/tests/models.smoke.test.js` (added this phase) exercises `user.getEnrollments()` → `.getCourse()` and `enrollment.getUser()` specifically to guard against this regressing.
5. **Association aliasing conventions** (for Phase 2+ consumers): belongsTo aliases strip a trailing `_id` from the FK column and camelCase it (`course_id` → `course`), except two columns that don't end in `_id` and got explicit aliases — `bank_transfer_proofs.reviewed_by` → `reviewer` (belongsTo) / `reviewedBankTransferProofs` (hasMany on User), and `announcements.created_by` → `creator` / `createdAnnouncements`; `audit_logs.actor_user_id` → `actor` (hasMany stays `auditLogs`). hasMany/hasOne aliases on the parent side are the pluralized/singularized child model name. `bank_transfer_proofs.order_id` is the one FK that's individually `UNIQUE`, so it's wired as `Order.hasOne(BankTransferProof, { as: 'bankTransferProof' })` rather than `hasMany`. Added one `belongsToMany` on top of the plain belongsTo/hasMany pairs: `MockExam.belongsToMany(Question, { through: MockExamQuestion, as: 'questions' })` and the reverse, since that junction table is a clean many-to-many (unlike `test_attempt_questions`, which carries per-attempt data and is intentionally left as plain hasMany/belongsTo per the brief's own example).
6. **Timestamp shape verified per table, not assumed uniform.** Despite the global `define: { underscored: true, timestamps: true }`, the literal DDL has three different timestamp shapes: `both` (created_at+updated_at, the default — no override needed), `createdOnly` (`login_events`, `one_time_tokens`, `lecture_bookmarks`, `playback_sessions`, `payment_events`, `notifications`, `audit_logs`, `question_bookmarks` — modeled with `updatedAt: false`), and `none` (`mock_exam_questions`, `test_attempt_questions`, `user_question_history`, `user_daily_stats` — modeled with `timestamps: false`). `settings` is the one outlier with `updatedOnly` (`createdAt: false`) and no surrogate `id` column at all — its `key VARCHAR(80)` is the literal primary key.
7. **Acronym-ish camelCase↔snake_case round-trip verified empirically, not assumed**, per the Phase 0 handoff's explicit ask: confirmed with a throwaway Sequelize model that `twofaEnabled↔twofa_enabled`, `twofaSecret↔twofa_secret`, `twofaBackupCodes↔twofa_backup_codes`, and `qbankSeconds↔qbank_seconds` all round-trip correctly under `underscored: true` — no `field:` overrides needed anywhere in the 34 models.
8. **Seeder idempotency: real skip-if-exists guards, not just a documented fresh-DB-only warning.** Each of the 10 seeder files (`server/src/db/seeders/2026010101000*-seed-*.cjs`, run in dependency order matching the migration grouping) checks for its own data before inserting — by unique natural key (email, slug, code, title, settings key) or by a count threshold (questions ≥ 200, faculty ≥ 3, faqs ≥ 6) — and logs `"...already present, skipping."` instead of inserting duplicates. Verified by running `npm run seed` twice back-to-back against the same seeded dev DB: the second run is a clean all-skip no-op (no errors, no duplicate rows). sequelize-cli's `db:seed:all` does **not** maintain its own tracking table (verified: no `SequelizeData` table exists after seeding) — idempotency here is entirely these per-file guards, which is also why they matter rather than being optional polish. `seed:undo` (via `sequelize-cli db:seed:undo:all`, wired as `npm run seed:undo` in both `server/package.json` and the root `package.json`) removes seeded rows by the same natural keys.
9. **Seed data specifics not fully spelled out by `03_DATABASE_SCHEMA.md`, resolved as follows:** (a) all 200 seeded questions use `exam_category = 'NRE1'` uniformly (matching the one seeded course/mock exam) rather than spreading across all 8 exam categories — the doc only asks for spread across subjects/systems/difficulties, and keeping one exam category keeps later QBank-meta/enrollment-scoped counts self-consistent; (b) question stems/explanations are generated from a small set of clinical-vignette-style templates parameterized by subject+system+case-number (see `20260101010003-seed-04-questions.cjs`) — templated but non-lorem-ipsum and structurally distinct per question, per the brief; (c) the mock exam's 50 questions are the first 50 seeded questions by id (deterministic, not random); (d) settings keys used for legal pages: `legal.privacy`, `legal.terms`, `legal.refund`, `site.about`, each a JSON object `{ title, content }` with clearly-labeled placeholder text ("replace with final legal copy before go-live") — no other settings keys were added (e.g. no payment/bank-detail settings yet — out of Phase 1 scope, deferred to Phase 9); (e) demo activity is 5 completed `practice`-mode test sessions (10 questions each, one per day for the last 5 days) with a deterministic PRNG (fixed seed, so re-running against a truly fresh DB reproduces the same numbers) targeting ~70% correct / 20% incorrect / 10% skipped per session; `user_question_history` and `user_daily_stats` are also populated from the same sessions (beyond what the brief's seed list explicitly named) since both tables exist specifically to power the analytics/dashboard screens the brief says this data is for — `questions.times_attempted`/`times_correct` were deliberately **not** touched, since the schema doc marks them "denormalized, cron-refreshed" (Phase 8.1's job), so 0 is the correct pre-cron seed state.
10. **Verification performed for real, not assumed:** `npm run migrate` (34/34 tables) → `npm run migrate:undo:all` (34/34 clean revert) → `npm run migrate` again (34/34 clean re-apply) against the live dev DB; `NODE_ENV=test npx sequelize-cli db:migrate --env test` against the live test DB (34/34 clean); `npm run seed` twice back-to-back (insert-then-skip, verified via `SELECT COUNT(*)`/integrity queries through the real `mysql.exe` CLI — every question has exactly 4 options and exactly 1 correct option); `server/tests/models.smoke.test.js` (new) exercises real model associations against a freshly-`globalSetup`-migrated test DB; root `npm run verify` (lint+test+build across server and client) is green with 0 lint errors (server: 0 warnings after the no-op-associate cleanup in point 4; client's pre-existing warnings are untouched, out of Phase 1 scope).

## 2026-07-31 — Phase 2 (backend-dev): auth, devices, sessions — verification pass + supertest coverage

**Context:** on picking up this task, `server/src/{config/constants.js, controllers/authController.js, middleware/{audit,auth,deviceCheck,rateLimits,requireRole}.js, routes/v1/auth.js, services/{auditService,authService,deviceService,twofaService}.js, utils/{asyncHandler,cookies,crypto,jwt,mailer,validate}.js}` already existed (untracked, from an earlier pass) implementing tasks 2.1–2.7 essentially completely and correctly against `docs/04_API_SPEC.md §1` / `docs/02_ARCHITECTURE.md §4` — model attribute names, envelope shape, layering, and the `geoip-lite`/`otplib`/`qrcode` dependencies (already added to `server/package.json` + installed) all checked out on read-through. `server/tests/` had zero coverage for any of it (only Phase 0/1's `health.test.js`/`models.smoke.test.js` existed). This entry covers: (a) the handful of real gaps found and fixed, (b) the full supertest suite added, (c) the testing-infrastructure decisions the suite needed.

1. **Two small real contract/test-ability gaps fixed in the existing implementation (not rewrites):**
   - `services/deviceService.js`'s `listDevicesForUser()` omitted `userId` from each device row. The already-built frontend's `UserDevice` type (`client/src/types/index.ts`) declares `userId: number` as required — per `CLAUDE.md §1a` ("prefer changing the backend to match the frontend's existing TypeScript types"), added `userId: d.userId` to the masked device projection. Confirmed via grep this is the only drift in the 2FA/QR/device-list contract: `setup2fa`'s `{qrCodeUrl, secret, otpauthUrl}` and `enable2fa`'s `{backupCodes}` already match `client/src/api/endpoints/auth.ts`'s `setup2FA()`/`enable2FA()` return shapes exactly (extra `otpauthUrl` field is harmless — TS callers just don't reference it), and `login`/`reverify`'s `{user, token, deviceToken}` shape (real session state lives in httpOnly cookies; `token`/`deviceToken` are non-secret placeholders — already documented in `authService.js`'s own comment) matches `LoginResponse` exactly. No frontend changes were made or needed.
   - `middleware/rateLimits.js`'s local `envelopeHandler` helper is now a named export. It was already referenced by name in that file's own header comment ("the limiter mechanism itself... is still exercised directly by tests/rateLimits.test.js") as a forward-reference to a test file that didn't exist yet — exporting it lets the new `server/tests/rateLimits.test.js` mount the *exact* production 429-handler code (not a re-implementation) on an isolated small-window limiter instance, since the real `/auth/*` limiters have `max` raised to 100000 under `NODE_ENV=test` and can't be tripped through the live app in-process without an impractical request burst.

2. **New: `utils/mailer.js` in-process `testOutbox`, gated on `NODE_ENV==='test'`.** The task brief suggested either nodemailer's JSON transport or "log the email to winston instead of sending" for the no-SMTP dev/test path (already implemented, via `jsonTransport`). Testing `verify-email`/`reset-password`/`reverify-login` end-to-end requires recovering the *raw* token/code a test just triggered — but the DB only ever stores a sha256 hash of it (by design), and the raw value only ever exists inside the composed email body. Considered `jest.unstable_mockModule('.../mailer.js', ...)` to intercept `sendMail` (this project's Jest config runs native ESM via `--experimental-vm-modules`, so that's the correct mocking primitive here) — rejected because mocking the whole module would also require re-implementing all five real template functions inside the mock factory (an ESM-mocked module replaces every export, and the factory can't cleanly re-import the "real" version of the same specifier it's mocking), which is more moving parts and more drift-risk than the alternative. Instead added `export const testOutbox = []` to `mailer.js` itself, pushed to only when `env.NODE_ENV === 'test'` (zero effect in dev/prod — always defined so the import never fails, never populated outside test). `server/tests/helpers/loginFlow.js` provides `extractReverifyCode`/`extractVerifyEmailToken`/`extractResetPasswordToken`, each searching the outbox most-recent-first by `to` + a subject-line regex specific to that template. This is a small, intentional test-only surface on production code (three lines), traded for a much simpler and more faithful test suite (exercises the *real* templates, not stand-ins).

3. **Test suite added — `server/tests/auth/{register,login,refresh,session,twofa,passwordReset,requireRole}.test.js` + `server/tests/rateLimits.test.js` (66 new tests total, all passing).** Structural decisions:
   - `server/tests/helpers/testUsers.js`: `createVerifiedUser()` creates an already-`active`/`emailVerifiedAt`-set user directly via the model layer (bcrypt cost 4, not the real `BCRYPT_ROUNDS=12`) for tests that need "some logged-in-able user," not a re-test of registration itself — `bcrypt.compare()` is cost-agnostic so this doesn't weaken what's being asserted, it just keeps a 66-test suite fast. Endpoints that hash a *new* password themselves (register, reset-password, change-password) always go through the real service code at its real cost — only initial test-fixture setup is cheapened.
   - `server/tests/helpers/loginFlow.js`: `loginNewDeviceAndReverify()` centralizes the "every brand-new device is suspicious by design" flow (see point 4 below) into one reusable helper (login → assert `REVERIFY_REQUIRED` → extract code → `POST /auth/reverify` → return an authenticated supertest agent), used by nearly every other test file. `getCookieValue()` parses a specific cookie's raw value out of a response's `Set-Cookie` headers — needed for the refresh-reuse-detection and cross-session-revocation tests, which must replay/inspect a *specific* raw token independent of a supertest agent's own auto-managed cookie jar.
   - **Confirmed as by-design, not a bug, and tested accordingly:** `authService.login()`'s suspicious-login check runs `matchResult.isNewCandidate` unconditionally, which is `true` for *any* device with no matching cookie/fingerprint row — including a user's very first login ever. So literally every first login from a given browser requires reverify-by-email, and only a *second* login from the same (now-registered) device completes directly. This is a literal, faithful implementation of the task brief's stated criterion ("flag as suspicious when: new device... OR...") with no carve-out for "first device ever," and mirrors a common real-world pattern (e.g. Google's "verify it's you" on any new device). The login "happy path" test therefore exercises a *second* login from an already-known device (via the `loginNewDeviceAndReverify` helper first), since that's the actual "everything's fine, cookies get set directly" case; the "brand-new device" behavior itself is asserted as its own explicit edge-case test.
   - **Device-limit / device-token-rotation interaction discovered while designing the "logout-all-other-sessions" test:** `deviceService.finalizeDevice()` rotates `deviceTokenHash` on *every* login completion, even for a reused/matched device row (not just newly-created ones). This means two independent logins that resolve to the *same* device slot (e.g. via fingerprint match after the cookie was cleared) will invalidate each other's `device_token` cookie for `deviceCheck` purposes — by design (it's the mechanism that lets a cleared-cookie device still be recognized safely), but it means "two concurrent sessions on the same device" isn't a realistic scenario to simulate with two independent cookie jars. The `change-password`/`logout-all` "other sessions get revoked, current one survives" tests therefore use **two distinct devices** (distinct `User-Agent` → distinct fingerprint, both within the 2-device cap) rather than two logins on one simulated device — a more realistic scenario anyway (user logged in on phone + laptop).
   - Every endpoint added in Phase 2 has at least: a happy-path test, a validation-failure (422) test where the endpoint takes a body, an auth-failure (401, or the endpoint's own most relevant failure mode for public endpoints) test, and the specific edge case named in the task brief's acceptance criteria (lockout-after-6, refresh rotation, reuse-detection-revokes-the-whole-family including the token that *was* still valid, 3rd-device-rejected-while-first-2-keep-working, 2FA-required-then-TOTP-succeeds, backup-code-single-use, no-enumeration on register/forgot-password/resend-verification, single-use one-time tokens on verify-email/reset-password, `requireRole` 403-for-wrong-role via a throwaway route, and the 429 envelope shape). `AuditLog` rows are asserted directly (not just "the request succeeded") for the two wirings the implementation chose (`user.logout_all`, `user.2fa_disable`).
   - Two trivial lint warnings introduced while writing the tests were cleaned up: an `eslint-disable-next-line no-await-in-loop` in `login.test.js` that this project's flat ESLint config doesn't actually enable (replaced with a plain comment explaining the intentional sequential loop), and an unused `DEFAULT_TEST_PASSWORD` import in `session.test.js`. A pre-existing identical "unused eslint-disable directive" warning in `services/twofaService.js` (not touched this session, already present in the code as handed off) was left as-is — out of this session's scope.

4. **Verification performed for real:** `npm run test --prefix server` → 10 suites / 66 tests, all passing (`server/tests/{health,models.smoke}.test.js` pre-existing + 8 new files). `npm run verify` (root: lint + test (server+client) + client build) → exit 0; server lint 0 errors / 1 pre-existing warning (see point 3's last bullet); client lint/build unchanged from before this session (its pre-existing warnings are out of scope, per the Phase 1 entry's own note). Jest prints a "did not exit one second after..." advisory after the full 10-file run (exit code still 0) — traced to `server/src/utils/logger.js`'s winston `File` transports never being explicitly closed, combined with Jest's per-test-file ESM module-registry isolation instantiating a fresh logger (and thus a fresh open file handle) per test file; reproduced that this warning does *not* appear when running only the two pre-existing test files, confirming it's a latent characteristic of `logger.js`'s design (no `close()`) that only crosses Jest's warning threshold once enough test files exist, not a bug introduced by anything in this phase's request/response code. Not fixed here — `logger.js` is Phase 0-owned infrastructure and out of this task's scope; flagged here for whoever next touches `logger.js` or the test harness.

## 2026-07-31 — Phase 2.8 security-auditor pass: 1 HIGH + 2 Medium + 2 Low fixed

**Context:** the security-auditor sub-agent ran against Phase 2's auth/device code and found 1 HIGH (phase-blocking), 2 Medium, 2 Low/cheap items, plus one informational item and two explicitly-skipped items (`login_events.email_tried` missing an index — scaling only, not exploitable; 2 wasted entropy bits in backup-code generation — still ~32 bits, rate-limited). This entry covers the HIGH/Medium/Low fixes and the judgment calls behind them.

1. **Finding 1 (HIGH) — device-cap bypass via UA fingerprint collision, fixed as a slot-takeover, not a capacity/enumeration fix.** `deviceService.matchDevice()` previously reused an existing `user_devices` row purely on `sha256(User-Agent+Accept-Language)` whenever no `device_token` cookie was presented, with **no signal distinguishing "cookie merely cleared on the same device" from "different device, same common fingerprint."** The auditor proved this lets an unrelated second/third login on a common UA/OS/locale combo (e.g. Windows+Chrome+en-US) silently reuse another session's device slot — no `DEVICE_LIMIT_REACHED`, no suspicious-login gate, and the **first session's refresh token stayed valid**, giving unlimited concurrent sessions per "slot."
   - `matchDevice()` now returns `isFingerprintOnlyMatch: Boolean(device) && !matchedByCookie` alongside the existing `isNewCandidate`, distinguishing three resolution states: (a) cookie hash matches an active row → verified same session, no special handling; (b) no cookie match but fingerprint matches an active row → **unverified** match, flagged; (c) no match at all → brand-new candidate (unchanged, still subject to the device cap).
   - **Judgment call — suspicious-login gate:** `authService.detectSuspicious()` previously only pushed `'new_device'` when `isNewCandidate` was true, which meant a fingerprint-only match was silently exempted from the reverify gate entirely (the actual root cause of "no suspicious-login gate" in the finding). Removed that exemption: `isFingerprintOnlyMatch` now also pushes a reason (`'device_takeover'`) into `detectSuspicious`'s result, so this path always requires email reverification before completing, identical treatment to a genuinely brand-new device. No carve-out was kept for "well, it's probably just a cleared cookie" — per the finding's own reasoning, the invariant that matters is "at most one live session per device slot," and reverify is cheap (the device's owner already has to have email access either way), so treating both cases identically is the safer default and avoids a second, parallel exemption.
   - **Judgment call — takeover mechanism:** `deviceService.finalizeDevice()` now revokes every currently-valid `refresh_tokens` row tied to that `device_id` (`WHERE deviceId = row.id AND revokedAt IS NULL`) whenever `isFingerprintOnlyMatch` is true, *before* the new session's refresh token is created — mirrors the existing device-token-rotation-on-every-completion behavior already documented in the Phase 2 entry above (point 3: "two independent logins that resolve to the same device slot... invalidate each other's device_token cookie... by design"), just extended to also invalidate the *refresh* token, which is the actual authentication artifact that mattered here. This does NOT increase the device cap check — it's a takeover of an *existing* row, not creation of a new one, so `assertDeviceCapacity` behavior for this path is unchanged (no-op for non-new candidates).
   - Deliberately did NOT extend `maybeSendNewDeviceAlert()`'s "new device" email to fire on `isFingerprintOnlyMatch` too (it still only fires for `isNewCandidate`). Not required by the finding, and since a takeover now always routes through the reverify-code email anyway, the account owner already gets a signal via that channel; expanding the separate "new device" alert email was judged out of scope for this pass rather than silently bundled in.
   - **Test/behavior fallout (expected, not a regression):** several pre-existing Phase 2 tests (`twofa.test.js`'s TOTP-happy-path and backup-code tests, `session.test.js`'s change-password test, `passwordReset.test.js`'s reset-password test) relied on the *old*, now-fixed shortcut — a fresh cookie-less `request(app)` call sharing a UA string with an already-registered device was completing directly instead of requiring reverify. Since that shortcut is the literal vulnerability, these tests were updated to reuse the already-authenticated `agent` (which carries the real `device_token` cookie) instead of a fresh cookie-less request, which is what each of those tests actually intends to exercise (2FA/password behavior on a known device, not device-matching semantics). No test was weakened to work around the fix; each was pointed at the right fixture.
   - New test: `server/tests/auth/login.test.js` — "two cookie-less logins with an identical UA fingerprint → second takes over the slot, revoking the first session." Asserts the second login succeeds (via the mandatory login→reverify flow, since it's now correctly flagged suspicious), the first session's raw refresh token is rejected by `/auth/refresh` afterward (`401 UNAUTHENTICATED`), and exactly one active `user_devices` row exists for the pair (slot reused, not duplicated). The pre-existing distinct-UA 3-device-cap test is untouched and still passes (different, still-valid scenario: it never presents no-cookie + colliding fingerprint).

2. **Finding 2 (Medium) — login timing side-channel, fixed with a bcrypt-cost-matched dummy compare.** `authService.js` now has a module-level `LOGIN_TIMING_PLACEHOLDER_HASH = bcrypt.hashSync('placeholder-not-a-real-password', BCRYPT_ROUNDS)`, computed once at load. The "no such user" branch of `login()` now runs `await bcrypt.compare(password, LOGIN_TIMING_PLACEHOLDER_HASH)` (result discarded) before logging/throwing, so it pays the same bcrypt-12 cost as the real "wrong password" branch. New regression test in `login.test.js` asserts the nonexistent-email path returns the identical `401 INVALID_CREDENTIALS` as the existing wrong-password test — deliberately a functional-correctness assertion only, no wall-clock timing assertion (would be flaky/pointless in a unit test, per the finding's own instruction).

3. **Finding 3 (Medium) — reverify brute-force lockout, added as an `isReverifyLockedOut()` mirroring `isLockedOut()`.** Reused the `login_events` table (no new table/migration needed) rather than a dedicated counter: every failed reverify attempt now writes a `login_events` row with `status='failed', reason='reverify_invalid_code'` (plus `status='blocked', reason='reverify_locked'` when the lockout itself trips), and a new `isReverifyLockedOut(emailTried)` counts those rows in the last `REVERIFY_LOCKOUT_WINDOW_MINUTES` (15, same window value as login lockout for consistency — see the existing "both windows share 15 minutes deliberately" note in `constants.js`) against a new `REVERIFY_FAIL_THRESHOLD = 5`. **Judgment call — lockout response code:** reused the existing `423 ACCOUNT_LOCKED` code rather than minting a new `04_API_SPEC.md` error code, since this is conceptually the same "too many failed attempts, wait it out" semantics as login's own lockout and `04_API_SPEC.md` doesn't otherwise enumerate a distinct reverify-lockout code — kept the contract surface minimal rather than adding a code no client currently branches on. **Judgment call — "lock out (or invalidate the pending token)":** chose lock-out-by-count (matching `isLockedOut`'s existing pattern) over invalidating the specific pending `one_time_tokens` row, since count-based lockout also protects against an attacker who requests a *fresh* suspicious login (and thus a fresh code) between guesses — invalidating only the current pending token wouldn't have stopped that. New test proves 5 wrong codes lock the account such that even the objectively-correct code is then rejected with `423 ACCOUNT_LOCKED` on the 6th attempt.

4. **Finding 4 (Low) — `jwt.verifyAccessToken()` now explicitly passes `{ algorithms: ['HS256'] }`** to `jwt.verify()` instead of relying on the library's inferred default. Zero behavior change (tokens were already signed HS256 by `signAccessToken()`).

5. **Finding 5 (Low) — added a per-IP companion limiter for `POST /auth/resend-verification`.** New `resendVerificationIpLimiter` (30/hour/IP, `RESEND_VERIFICATION_MAX_PER_IP_PER_HOUR` in `constants.js`) stacked in front of the existing email-keyed `resendVerificationLimiter` on that one route, so a single IP cycling through many different target emails is now bounded independently of the email-keyed limit. No new test added for this one specifically — consistent with the existing pattern in this codebase (`rateLimits.js`'s own header comment / the Phase 2 entry above): production `/auth/*` limiters have `max` raised sharply under `NODE_ENV=test` so they can't be tripped for a real 429 through the live app in-process, and the 429-envelope mechanism itself is already covered generically by `tests/rateLimits.test.js`.

6. **Informational item — `models/User.js` now overrides `toJSON()`** to strip `passwordHash`/`twofaSecret`/`twofaBackupCodes` as a defense-in-depth safety net for any future accidental `res.json(user)`/`JSON.stringify(user)` (every current response path was already verified safe via explicit `authService.serializeUser()`, so this wasn't an active leak). Verified safe: every existing direct-property access (`user.passwordHash`, `user.twofaSecret`, `user.twofaBackupCodes` — in `authService.js`, `twofaService.js`, and several tests) is unaffected, since overriding `toJSON()` only changes `JSON.stringify()`/`res.json()` behavior, not direct property reads.

7. **Verification performed for real:** `npm run test --prefix server` → 10 suites / **69** tests, all passing (66 pre-existing + 3 new: nonexistent-email timing regression, UA-fingerprint-collision device-takeover, reverify-lockout). Root `npm run verify` (lint + server/client test + client build) → exit 0; server lint 0 errors (same 1 pre-existing warning as before, untouched); client lint/build unchanged, pre-existing warnings out of scope.
