# SAMS Academy client — no build-time environment variables are required.
#
# CONFIG.API_BASE_URL ("/api/v1") in src/config.ts is hardcoded same-origin:
# Express serves client/dist and the API from the same host/port in every
# environment, so there is nothing to configure here. See root .env.example
# for the server-side environment variables.
