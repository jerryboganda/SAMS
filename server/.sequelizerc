// server/.sequelizerc — sequelize-cli path overrides.
// NOTE: plain CommonJS on purpose (see server/src/db/config.cjs comment) —
// sequelize-cli require()s this file directly.
const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'db', 'config.cjs'),
  'migrations-path': path.resolve(__dirname, 'src', 'db', 'migrations'),
  'seeders-path': path.resolve(__dirname, 'src', 'db', 'seeders'),
  'models-path': path.resolve(__dirname, 'src', 'models'),
};
